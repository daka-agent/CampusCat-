# 2026-05-08 工作日志

## GitHub Pages 迁移

完成了将 CampusCat 项目从腾讯云服务器迁移到 GitHub Pages 的准备工作：

1. **新建** `.github/workflows/deploy.yml` — GitHub Actions 自动部署工作流，push 到 main 分支即自动触发 GitHub Pages 部署
2. **更新** `README.md` — Demo 链接从腾讯云 IP 占位符替换为 GitHub Pages 地址，新增部署徽章
3. **更新** `docs/DEPLOY.md` — GitHub Pages 章节升级为 Actions 自动化方式

本地提交成功（commit `5da9cfb`），推送时因 GitHub PAT 缺少 `workflow` 权限被拒绝。
需用户手动在 GitHub 网页上创建 `deploy.yml` 文件，并在 Settings → Pages 中启用 GitHub Actions。
